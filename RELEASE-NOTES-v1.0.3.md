# v1.0.3 更新说明

本次更新重点修复画布一键分镜的分集识别、内容保存和生成效率问题，并增强长任务的可恢复性。

## 修复内容

- 扩充分集标题识别，支持中文数字、阿拉伯数字、全角数字、带括号数字、圆圈数字、序号前缀和从中间集开始的剧本。
- 修复 Markdown 加粗或碎片化格式干扰分集识别的问题，剧本仍按肉眼可见的集数标题拆分。
- 修复一键分镜期间旧画布快照覆盖新结果、刷新后生成进度丢失、剧本节点内容被清空等问题。
- 生成结果按集逐个保存；部分集已完成时，即使后续请求失败，已完成内容也不会丢失。
- 同一剧本生成期间禁止重复发起相同任务，服务重启造成任务中断时会显示明确失败状态，不会一直停留在“生成中”。
- 修复相邻同角色短台词拆成多个镜号时反复调用模型的问题，能够在本地合并可修复的短台词。
- 所有发给大模型的文本请求会先移除仅供界面展示的 Markdown 标记，模型返回内容仍保留原有展示能力。

## 生成效率

- 分镜生成使用 `deepseek-v4-flash` 时关闭思考模式，限制单次输出和超时时间。
- 最多同时生成两集；接口限流时自动降为单集顺序生成。
- 每集最多尝试两次，并在画布节点上显示当前集数和尝试次数。
- 同一份第 13 集至第 17 集剧本，旧流程耗时约 39 分 31 秒；优化后自动测试耗时 86.575 秒，页面手动点击耗时 87.261 秒。

## 验证结果

- 自动测试：291 项通过，0 项失败。
- 页面手动生成：5 集全部落盘，最终硬规则问题数均为 0。
- 生成前后源剧本长度和 SHA-256 一致，刷新页面后分镜结果仍然保留。

## 更新包

- 本次提供 `MbhStoryboardScriptAssistant-CustomerTrial-NoSkillOverwrite-v1.0.3.zip`。
- 更新包不包含 `skills/`，不会覆盖客户已有技能。
- 该包适合覆盖到已有完整安装目录；若目标目录没有 `skills/`，请先使用包含技能的完整包安装。
