# 对话学习记录

本目录用于保存后续对话中自动沉淀的学习记录。

使用原则：

- 普通问答、临时状态询问、无项目增量的信息，不必记录。
- 用户提供新的偏好、流程要求、质量反馈、题材规律、样例解释时，应记录。
- 用户指出“学坏了”“质量下降”“不如之前”等信号时，应记录，并同步生成降质和回退记录。
- 记录只作为学习证据入口，不等于直接修改正式 skill。

推荐由 `tools/New-ConversationLearningRecord.ps1` 生成记录。
