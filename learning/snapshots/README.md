# 学习闭环快照

这里存放项目每轮学习后的总览快照。

快照用于回答：

- 当前有哪些样例和运行证据。
- 哪些 M4 任务已经完成。
- 候选规则和已确认规则分别在哪里。
- 下一步是否可以进入技能进化。

生成命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "tools/New-LearningSnapshot.ps1" -Force
```
